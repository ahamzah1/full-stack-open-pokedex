name: Hello World!

on:
  push:
    branches:
      - main


jobs:
  hello_world_job:
    runs-on: ubuntu-22.04
    steps:
      - name : say Hello
        run: echo "Hello World"
      - name: Now it is
        run: Date
      - name: Directory content
        run: ls -l